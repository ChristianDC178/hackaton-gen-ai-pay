# This workflow is disabled - using Amplify native CI/CD instead
# Amplify will automatically detect changes in frontend/ folder
# and deploy using the amplify.yml configuration

# To enable this workflow:
# 1. Add AWS_ACCESS_KEY_ID secret to GitHub repository
# 2. Add AWS_SECRET_ACCESS_KEY secret to GitHub repository  
# 3. Add AMPLIFY_APP_ID secret to GitHub repository
# 4. Uncomment the workflow below

# name: Deploy to AWS Amplify
# on:
#   push:
#     branches: [ main, master ]
#     paths:
#       - 'frontend/**'
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#     - uses: actions/checkout@v4
#     - uses: actions/setup-node@v4
#       with:
#         node-version: '18'
#         cache: 'yarn'
#         cache-dependency-path: frontend/yarn.lock
#     - run: cd frontend && yarn install --frozen-lockfile
#     - run: cd frontend && yarn build